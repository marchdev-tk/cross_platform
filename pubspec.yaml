name: cross_platform
version: 1.0.0
homepage: https://github.com/marchdev-tk/cross_platform
description: Cross platform package that unifies the work with Platform and wrapps dart:io Platform and dart:html to determine platform of the host machine.

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  universal_html: ^1.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  pedantic: ^1.11.0

flutter:
  uses-material-design: true
